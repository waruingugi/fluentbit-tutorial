service:
  flush: 1
  log_level: info

pipeline:
  inputs:
    - name: tail
      path: /logs/django.log
      tag: django.app
      read_from_head: true

  outputs:
    - name: stdout
      match: 'django.*'
      format: json_lines